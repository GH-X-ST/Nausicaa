[project]
name = "nausicaa"
version = "0.1.0"
description = "Design and optimisation workflow for the Nausicaa indoor glider."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aerosandbox>=4.2,<5",
    "numpy>=1.24",
    "pandas>=2.0",
    "matplotlib>=3.7",
    "openpyxl>=3.1",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"