"""retired_not_active: old contextual dense runner.

The active evidence workflow is implemented by the LQR runner entrypoints:
`run_lqr_tuning_sweep.py`, `run_lqr_contextual_archive.py`,
`run_lqr_w2_replay.py`, and `run_lqr_w3_generalisation.py`.
"""

from __future__ import annotations


def main() -> int:
    raise SystemExit(
        "retired_not_active: use the active LQR dense evidence runners instead."
    )


if __name__ == "__main__":
    main()

