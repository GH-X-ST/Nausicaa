First inspect the local repo history, tags, and existing docs/PR wording for recent title/version patterns. Propose a concise high-level PR title with a version label that continues the existing project sequence rather than inventing a fresh v1.0. Use the same title family when this change extends the same development line; increment minor versions for scaffold/refinement work, and reserve major versions for completed evidence milestones or major claim-boundary changes.

<!-- R9_LAUNCH_GATE_ALIGNMENT_START -->
## Current controlling update — robust R5 synthesis and frozen W3 holdout

This document is aligned with `docs/Glider_Control_Project_Plan.md`, which is the single method-control document for the current implementation pass.

Current decision:

```text
BLOCKED_FIX_REQUIRED
```

The current R9 repeated-launch result and W3 run 015 launch-capture result are diagnostic evidence only. They must not be used to claim memory improvement, hardware readiness, real-flight transfer, mission success, or full autonomy. The repair is now to improve launch-capture synthesis quality inside robust randomized R5 W0/W1 training: references, LQR weights, launch_gate start subregimes, handoff-quality gates, and the R9/R10 lookup and selection-change metrics.

Required repair direction:

```text
1. Keep the dedicated LQR-only launch-capture primitive family, but redesign or widen its references, weights, and launch_gate start subregimes inside R5 robust synthesis.
2. Keep existing in-flight primitive families intact; do not relabel all in-flight primitives as launch-capable.
3. Preserve the active primitive contract: 0.100 s horizon, 5 controller-input slots, 20 ms controller update period.
4. Preserve primitive-local time-invariant LQR only; no PD/PID/bounded fallback and no TVLQR active workflow.
5. Use W2 only as optional diagnostic/legacy comparison evidence, not as a required pass gate.
6. Keep frozen W3 as fixed-LQR held-out validation with no Q/R, K, reference, horizon, entry-role, controller-ID, or primitive-variant-ID mutation.
7. Keep post-W3 reduction as the four-case library-size cross-study: heavy, balanced, light, and no-clustering/no-merging.
8. Fix R9 outcome lookup so representatives are matched by library-size case and compact-library identity, not only by primitive_variant_id.
9. Compute memory_changed_selection and exploration_changed_selection from actual matched selections; do not hard-code them to zero.
10. Add first-decision launch-gate audits for candidate count, viable count, rejection reason, selected family, and transition-to-inflight evidence.
11. Regenerate evidence from the corrected R5 W0/W1 robust synthesis run before reusing R7, R8, R9, or R10 evidence.
12. Treat W3 run 015 as diagnostic_not_passed for launch-aware move-on because launch_capable variants are 0 survived / 192 downgraded.
```

Forbidden changes remain unchanged: no fan-layout-specific controller logic, no pre-W3 clustering, no W3/R8/R9/R10 retuning, no deletion of useful x-y terminal boundary evidence, no direct surface-command RL, no nonlinear MPC substitution, and no LQR-tree/funnel-library claim.
<!-- R9_LAUNCH_GATE_ALIGNMENT_END -->

Then write a short GitHub push or PR comment in this style:

- Language or area.
- High-level intent.
- Key interfaces, assumptions, or evidence boundaries clarified, including whether evidence concerns W0/W1 primitive-local variants, W2/W3 survival filtering, post-W3 library compression, or late validation.
- Tests or checks run, if any.
- Claims explicitly not made, if relevant.

Do not list every file changed. Keep it general, audit-focused, and suitable for a robotics research codebase.

Example:

```text
Glider Design v5.6
```

```text
Python control-simulation pipeline

- Adds environment-context fields for primitive outcome evidence, so fan layout and fan power are represented as local flow features rather than controller branches tied to fan layout.
- Preserves state/command conventions, latency labels, viability filtering, and simulation-only claim boundaries.
- Dense outputs remain chunked, compressed, resumable, and partitioned below the 100 MB project file limit.
- Checks run: fixed-gate archive tests, dense runtime tests, and git diff --check.
- No real-flight transfer, mission success, or hardware-readiness claim is made by this change.
```

Current R9 launch-gate repair example:

```text
LQR-Stabilised Contextual Primitive v5.1
```

```text
Python control-simulation pipeline

- Repairs the blocked R9 repeated-launch validation path by adding launch-gate coverage diagnostics and a project-aligned launch-capture primitive repair plan.
- Preserves the primitive-local time-invariant LQR workflow, 0.100 s / 5-slot / 20 ms timing contract, directional 3D residual memory, safe exploration/exploitation, and post-W3 heavy/balanced/light/no-clustering/no-merging library-size study.
- Clarifies that current R9 evidence is diagnostic_not_passed until launch-capture coverage, outcome lookup, and selection-change metrics are repaired and evidence is regenerated from R5.
- Checks run: source audit, relevant unit tests, R5-R10 gate checks, and R9 launch-gate audit where applicable.
- No memory improvement, hardware readiness, real-flight transfer, mission success, or full autonomy claim is made by this change.
```

Current robust-synthesis repair example:

```text
LQR-Stabilised Contextual Primitive v5.6
```

```text
Python control-simulation pipeline

- Reframes the launch-capture repair around robust randomized R5 W0/W1 synthesis, where primitive-local references and LQR Q/R are tuned before freezing the controller bundle.
- Makes W2 optional diagnostic evidence only, and keeps W3 as the frozen held-out validation stage with seed separation, active-fan-count variation, CG offset, and left/right aileron asymmetry captured in the audit boundary.
- Preserves role separation: launch_capture_* primitives are launch_capable, glide and ordinary manoeuvres are inflight_only, and recovery/safe-exit handoff primitives are terminal_or_recovery; post-W3 compression keeps the heavy/balanced/light/no-clustering cases.
- Late validation still uses directional 3D residual memory histories 0, 5, 10, 20, 50, and 100, safe exploration/exploitation after viability filtering, controlled final-launch pairing, and the launch-score audit as secondary analysis only.
- No memory improvement, hardware readiness, real-flight transfer, mission success, or full autonomy claim is made until full R9/R10 gates pass.
```
