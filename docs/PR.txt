First inspect the local repo history, tags, and existing docs/PR wording for recent title/version patterns. Propose a concise high-level PR title with a version label that continues the existing project sequence rather than inventing a fresh v1.0. Use the same title family when this change extends the same development line; increment minor versions for scaffold/refinement work, and reserve major versions for completed evidence milestones or major claim-boundary changes.

<!-- R9_LAUNCH_GATE_ALIGNMENT_START -->
## Current controlling update - robust R5 synthesis and frozen W3 holdout

This document is aligned with `docs/Glider_Control_Project_Plan.md`, which is the single method-control document for the current implementation pass.

Current decision:

```text
BLOCKED_FIX_REQUIRED until the repaired R5 -> R7 -> R8 -> R9 -> R10 -> R11 evidence chain passes, with R6 archived diagnostic-only and R11 as the strict held-out validation gate.
```

Required repair direction:

```text
1. Restart accepted evidence from robust R5 dry-air plus annular-GP synthesis; older R5/R7/R8/R9/R10/R11 roots remain diagnostic unless regenerated under the current contract.
2. R5 dry-air rows keep the W0 no-updraft environment but use W3-style plant and implementation perturbations; R5 annular-GP rows use the same W3-style plant/implementation perturbation family.
3. R5 and R7 use single-layer composed annular-GP randomisation: fan position, active fan count where applicable, per-fan power, width/spread, local uncertainty, launch/start-state perturbations, latency/implementation perturbations, left/right aileron asymmetry, and CG/plant perturbations.
4. Do not duplicate randomisation channels: per-fan power is updraft strength, fan position is spatial/layout shift, width is spread, local uncertainty is context/belief uncertainty, amplitude stays fixed at 1.0, and centre shift stays zero for active pass-gated annular-GP evidence.
5. Keep W2/R6 archived optional diagnostic only; R7 is the frozen held-out W3 fixed-LQR replay with no Q/R, K, reference, horizon, entry-role, controller-ID, or primitive-variant-ID mutation.
6. Keep post-W3 reduction as the five-case coverage-aware medoid library-size cross-study: heavy, balanced, light, super-light, and no-clustering/no-merging. R8 selects existing W3-surviving variants by hard safety filtering, per-case coverage, behavior/Q-R medoid centrality, and greedy marginal coverage, without averaging Q/R or synthesising new controllers.
7. R9 is an internal reduced fixed-case preflight with no glider, latency, actuator, or environment randomisation beyond controlled launch/history sampling; it uses two no-updraft, two single-fan, and two four-fan outer cases per policy/history condition to produce an initial governor handoff for R10. R9 is not formal thesis evidence and is not the final claim gate.
8. R10 is environment-only changed-case governor tuning with six 20-case blocks: nominal single, nominal four, shifted single, shifted four, active-fan-number variation, and arena-wide fan-position plus active-fan-count generalisation. R10 consumes the R9 initial governor handoff when available, writes a frozen governor-configuration handoff for R11, and R11 is the strict held-out viability-governor validation gate.
9. R9/R10/R11 must use controlled final-launch pairing, compact-library-aware outcome lookup with positive numeric `sample_count` coverage, actual memory/exploration selection-change metrics, and secondary launch-score analysis. R11 must apply the same changed-case fan-count and fan-position semantics as R10.
10. No memory improvement, hardware readiness, real-flight transfer, mission success, full autonomy, or formal LQR-tree/funnel/ROA claim is allowed until the strict R11 `safe_success` and `full_safe_success` gates pass.
```

Forbidden changes remain unchanged: no PD/PID/bounded fallback, no TVLQR active workflow, no fan-layout-specific controller logic, no pre-W3 clustering, no W3/R8/R9/R10/R11 retuning, no deletion of useful x-y terminal boundary evidence, no direct surface-command RL, no nonlinear MPC substitution, and no LQR-tree/funnel-library claim.
<!-- R9_LAUNCH_GATE_ALIGNMENT_END -->

Then write a short GitHub push or PR comment in this style:

- Language or area.
- High-level intent.
- Key interfaces, assumptions, or evidence boundaries clarified, including whether evidence concerns W0/W1 primitive-local variants, W2/W3 survival filtering, post-W3 library compression, or late validation.
- Tests or checks run, if any.
- Claims explicitly not made, if relevant.

Do not list every file changed. Keep it general, audit-focused, and suitable for a robotics research codebase.

Example:

```text
Glider Design v5.6
```

```text
Python control-simulation pipeline

- Adds environment-context fields for primitive outcome evidence, so fan layout and fan power are represented as local flow features rather than controller branches tied to fan layout.
- Preserves state/command conventions, latency labels, viability filtering, and simulation-only claim boundaries.
- Dense outputs remain chunked, compressed, resumable, and partitioned below the 100 MB project file limit.
- Checks run: fixed-gate archive tests, dense runtime tests, and git diff --check.
- No real-flight transfer, mission success, or hardware-readiness claim is made by this change.
```

Current R9 launch-gate repair example:

```text
LQR-Stabilised Contextual Primitive v5.1
```

```text
Python control-simulation pipeline

- Repairs the blocked R9 repeated-launch validation path by adding launch-gate coverage diagnostics and a project-aligned launch-capture primitive repair plan.
- Preserves the primitive-local time-invariant LQR workflow, 0.100 s / 5-slot / 20 ms timing contract, directional 3D residual memory, safe exploration/exploitation, and post-W3 heavy/balanced/light/super-light/no-clustering/no-merging library-size study.
- Clarifies that current R9 evidence is diagnostic_not_passed until launch-capture coverage, outcome lookup, and selection-change metrics are repaired and evidence is regenerated from R5.
- Checks run: source audit, relevant unit tests, R5-R10 gate checks, and R9 launch-gate audit where applicable.
- No memory improvement, hardware readiness, real-flight transfer, mission success, or full autonomy claim is made by this change.
```

Current robust-synthesis repair example:

```text
LQR-Stabilised Contextual Primitive v5.6
```

```text
Python control-simulation pipeline

- Reframes the launch-capture repair around robust randomized R5 dry-air plus annular-GP synthesis, where primitive-local references and LQR Q/R are tuned before freezing the controller bundle.
- Makes W2 optional diagnostic evidence only, applies W3-style plant/implementation perturbations even to R5 dry-air rows, and keeps W3 as the frozen held-out validation stage with seed separation, active-fan-count variation, CG offset, and left/right aileron asymmetry captured in the audit boundary.
- Preserves role separation: launch_capture_* primitives are launch_capable, glide and ordinary manoeuvres are inflight_only, and recovery/safe-exit handoff primitives are terminal_or_recovery; post-W3 compression keeps the heavy/balanced/light/super-light/no-clustering cases.
- Late validation still uses directional 3D residual memory histories 0, 5, 10, 20, 50, and 100, safe exploration/exploitation after viability filtering, controlled final-launch pairing, R9 fixed cases, R10 six changed-environment blocks including arena-wide fan-position plus active-fan-count generalisation, and the launch-score audit as secondary analysis only.
- No memory improvement, hardware readiness, real-flight transfer, mission success, or full autonomy claim is made until strict R11 gates pass.
```
