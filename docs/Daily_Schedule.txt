DAILY SCHEDULE - ROBUST R5 DRY+ANNULAR-GP SYNTHESIS -> FROZEN W3 HOLDOUT -> POST-W3 LIBRARY-SIZE CROSS-STUDY -> INTERNAL R9 PREFLIGHT -> R10/R11 VALIDATION

<!-- R9_LAUNCH_GATE_ALIGNMENT_START -->

## Active Transition-Aware Thesis Workflow

The active thesis workflow is `R5 -> R7 -> R8 -> R10 -> R11 -> Reality`. R9 remains internal preflight only and is not thesis-facing evidence. R10 tunes the viability governor with residual updraft adaptation, and R11 is the held-out validation gate.

Launch is an entry regime, not a separate controller family. The active primitive catalogue has exactly eight manoeuvre families: `glide`, `recovery`, `lift_entry`, `lift_dwell_arc`, `mild_turn_left`, `mild_turn_right`, `energy_retaining_bank`, and `safe_exit_or_recovery_handoff`. Retired `launch_capture_*` IDs are archive aliases only and must not appear in active evidence. `safe_exit_or_recovery_handoff` remains active as an evidence-tested recovery / controlled-terminal primitive; it must be demoted only if R8/R10 evidence shows no unique transition coverage, not removed by assumption.

Every primitive is treated as a transition object. R5 is robust transition-aware primitive / transition-object Q/R plus primitive attitude/bank reference-bias training across five entry start families with exact dense proportions per primitive/candidate/environment: 40 `launch_gate`, 25 `inflight_nominal`, 15 `inflight_lift_region`, 10 `inflight_boundary_near`, and 10 `inflight_recovery_edge`. The dense evaluation target remains `8 * 32 * 3 * 100 = 76,800` rows, but row count alone is not a pass condition. Candidate 0 is nominal, candidates 1-7 are named physical anchors with small interpretable attitude/bank reference biases, and candidates 8-31 are deterministic Latin-hypercube log multipliers over the seven grouped LQR weights plus bounded pitch and bank reference biases. In-flight start-state velocity envelopes cover most of the local-speed scheduling grid: nominal `u=3.0--8.2`, lift-region `u=3.2--8.0`, boundary-near `u=3.0--8.0`, and recovery-edge `u=2.2--5.2` m/s, with wider lateral/vertical body-velocity perturbations logged by the sampler. Speed-bin selection is for local model scheduling only; active primitives must not chase speed as a hard reference. R5 writes `r5_transition_candidate_training_summary.csv`, `r5_transition_selected_for_r7.csv`, `r5_transition_pareto_front.csv`, and `r5_transition_training_manifest.json`, then freezes only selected transition objects for R7 while keeping the full candidate bundle as audit evidence.

R7 is held-out transition validation of the frozen R5-selected transition objects. No primitive may pass R7 solely on local rollout success; no primitive may pass R5 or R7 from dense row count or aggregate primitive success across entry classes. R7 uses entry-class-specific labels: `survived` is reserved for strict high-probability `inflight_stable` evidence, `route_usable` keeps `launch_gate` evidence when transition probability is at least 0.40 with near-zero hard failure and keeps `boundary_near` evidence when transition probability is at least 0.40 with hard failure below limit, and `recovery_route_usable` keeps `recoverable_degraded` evidence when it has nonzero recovery progress in dry-air, single-fan, and four-fan R7 modes with low hard failure. For recovery starts, `recoverable_degraded -> recoverable_degraded` remains a conditional route pass when attitude/rate risk improves, front/side boundary time margin does not collapse, floor margin does not collapse, and hard-failure risk remains low; `recoverable_degraded -> boundary_near` is reported as route/weak evidence, not a full pass. A controller can be strict-surviving for one `primitive_id + entry_class`, route-usable for another, and fail for another. R8 compresses R7/W3-eligible transition objects (`survived`, `route_usable`, and `recovery_route_usable`) grouped by `primitive_id` and `transition_entry_class` using coverage-aware medoid selection without averaging Q/R, K, references, or controller IDs. R8 must also preserve distinct W3-eligible local LQR speed-bin coverage within each primitive/entry-class group up to the case representative budget; speed-bin collapse is a library-coverage failure, not an LQR-principle failure.

The governor classifies the current state, filters representatives by validated `transition_entry_class`, rejects high hard-failure risk, and scores already-admissible candidates using transition/terminal probability, hard-failure risk, candidate-path front-wall progress, a front-wall terminal proxy, progress-gated terminal total specific-energy proxy, wrong-boundary penalty, context-conditioned updraft gain, lift-dwell time, and candidate-specific residual-memory correction. Airborne or flight time is audit-only and must not be a selector or launch-score reward. Step 0 has `current_state_class = launch_gate`, so it selects only transition objects validated for `entry_class = launch_gate`; there is no launch-specific primitive family route. Because repeated-launch episodes must end somewhere, a finite controlled x-y arena exit with positive floor/ceiling margin is a `safe_terminal` outcome, not a hard failure. For final R9/R10/R11 launch scoring, `mission_success` is stricter than generic `safe_terminal`: the held-out rollout must exit through the front wall at `x_w = 6.6 m` with y/z inside the true safe bounds, while wrong-wall exits, floor/ceiling impact, invalid state, uncontrolled attitude/rates, and no-viable tails remain penalties or failures.

The inner LQR remains a stabilising tracker around the primitive-defined local reference. It must not become the manoeuvre planner: primitive transition objects define manoeuvre intent and R5/R7/R8 decide which transition objects are valid, while the governor selects among those frozen transition objects. Signed turn intent is diagnostic only: R5/R7 may record signed bank, signed roll-rate, and lateral turn tendency for audit, but active selection must not reward turn-expression strength and `mild_turn_left` / `mild_turn_right` must not receive sign-constrained bank or roll-rate reference forcing. `mild_turn_left` and `mild_turn_right` remain separate directional primitive IDs because the arena, local flow, and reachable transitions are not guaranteed symmetric; R8 may compress or downweight their representatives by evidence, but active code must not merge the IDs into one aggregate score. `energy_retaining_bank` remains non-directional because it is an energy/posture primitive, not an explicit left/right command. Same-start trajectory plots are diagnostic-only sanity checks for controller behaviour and do not replace R5/R7 transition evidence.

Residual memory is a small case-local modifier: `predicted_updraft_gain = library_prediction + residual_memory_correction`. The active R9/R10/R11 outer-loop policy is candidate-specific, recency-weighted, baseline-shielded, and mission-aligned: each already-viable primitive receives candidate-path front-wall/energy utility plus a capped residual correction and a small uncertainty-directed exploration term queried along its predicted current/midpoint/exit path, but an adaptive winner is accepted only when it clears score-margin and non-regression checks against the no-memory winner. Pure uncertainty exploration is limited to the baseline primitive family; cross-family switches require confident candidate-path memory evidence rather than curiosity alone. Memory uses launch-index half-life recency, so the most recent few history launches dominate while older launches remain weak prior evidence. The same shield is applied at every repeated-launch decision; the code does not branch on a launch being a known final mission. It must not override state classification or entry/exit compatibility. Memory is reinitialised per final test row. Final scoring is computed only from the final held-out rollout path: R9/R10/R11 launch score rewards front-wall mission completion, capped updraft-gain and lift-dwell evidence, and terminal total specific-energy reserve after front-wall success. Airborne time and generic net/gross energy drift remain audit-only. There is no hidden speed gate, no energy-loss hard failure, no PD/PID, no TVLQR, and no fan-layout-specific controller logic.

The repeated-launch outer-loop scheduler must not assume free real-time computation. R9/R10/R11 profile context construction, residual-memory query, and compact-library selection for every primitive decision against a preferred 20 ms controller-slot budget and a hard 0.100 s primitive-boundary budget. Step 0 may be prepared before release; later steps use a prepared next-decision path so the next primitive is committed at the boundary while actuator/command FIFO history remains continuous. This is an offline wall-clock profile only, not a hardware real-time-readiness claim.

R10 and R11 have separate roles. R10 is governor/residual-memory tuning on one hard training distribution, `r10_l7_full_domain_randomisation_arena_wide_training`, with 50 outer cases per condition: four-fan geometry, active fan count 0/1/2/3/4, fan parameter uncertainty, arena-wide non-overlapping fan positions, and W3 plant/implementation perturbations. Arena-wide fan positions are rejection-sampled inside the tracker footprint with a 0.5 m safety radius around each fan, so fan-centre distances must be at least 1.0 m and safety circles do not overlap. Between R10 history launches, launch state and environment/fan realisations are resampled, but glider physical parameters, latency, and actuator implementation are sampled once per outer case and held fixed to keep memory learning interpretable. R11 is held-out validation on an eight-block fidelity ladder with 10 outer cases per block, 80 total: L0 dry-air fixed, L1 single-fan fixed nominal, L2 four-fan fixed nominal, L3 fan-parameter uncertainty, L4 local fan-position uncertainty, L5 active-fan-count uncertainty 0/1/2/3/4, L6 environment-only full uncertainty, and L7 full-domain arena-wide randomisation. R11 L0-L2 keep the environment fixed between history episodes; L3-L7 resample environment/fan/launch histories, with plant/implementation perturbations only in L7 and still fixed per outer case. Core comparison is no memory versus recency-weighted candidate-path residual-memory histories h3, h10, and h30 with built-in shielded uncertainty exploration; h100 and standalone safe-explore policy variants are optional appendix ablations. Hardware readiness, real-flight transfer, mission success, autonomy, and memory-improvement claims require R11 and later real-flight evidence.

<!-- R9_LAUNCH_GATE_ALIGNMENT_END -->

Assumption
----------
The repository restarts from the retained foundation only:

- glider / flight-dynamics model;
- panel-wise aerodynamic / local-flow model;
- feedback-latency, command-timing, and actuator-lag models;
- updraft / local-flow model;
- runtime and storage utilities;
- model tests and current project documentation.

Retired fan-layout-specific archive code, chain-formation logic, stale result archives, stale clustering packages, stale policy outputs, stale Codex-generated plans, and the first bounded-PD/contextual-feedback dense run are not part of the active method. The previous dense run must be archived or deleted before the new LQR dense run is started.

Definitions
-----------
- Primitive: short LQR-stabilised manoeuvre with entry checks, finite horizon, exit checks, metrics, and failure labels.
- LQR controller: time-invariant local feedback controller embedded inside one primitive, identified by `controller_id`, `linearisation_id`, Q/R weights, gain checksum, and closed-loop audit status.
- Primitive-controller variant: the active library item combining one primitive entry set, reference state/command, finite horizon, local LQR Q/R/K, exit checks, failure labels, and claim status.
- Environment context: local flow and safety features computed from the current state and wind field.
- Episode: one simulated or physical launch from the fixed nominal launch gate.
- R5 W0/W1 robust transition-object Q/R plus primitive attitude/bank reference-bias training: the heavy primitive-library creation and tuning stage. W0 uses dry/near-dry no-updraft environment rows while still applying W3-style plant and implementation perturbations. Active W1 pass-gated evidence uses annular-GP randomized single-fan and four-fan training blocks, with active-fan-count variation where supported. Axisymmetric Gaussian plume is diagnostic-only. Each primitive-controller variant uses the structured 32-candidate Q/R plus attitude/bank reference-bias method, local-speed scheduled LQR linearisation, and entry-by-entry transition selection using transition success lower bounds, hard-failure upper bounds, worst-environment success, updraft-gain proxy, lift dwell, rollout duration, and saturation. Longitudinal speed is a scheduling variable and passive command-error channel, not a hard primitive target. All solved, blocked, weak, failed, rejected, continuation-valid, and x-y terminal-useful rows are preserved as audit evidence, but only R5-selected transition objects are frozen for R7.
- W2 diagnostic sweep: optional fixed-LQR replay using the single-fan and four-fan GP-corrected annular-Gaussian surrogate (`annular-GP` in the repository), panel-wise wind, feedback latency, command timing, actuator lag, and realistic termination. No hidden LQR retuning. W2 is diagnostic/legacy comparison evidence only, not a required pass gate.
- R7 dry-air plus W3 frozen holdout sweep: fixed-LQR replay under held-out domain randomisation of updraft parameters, fan positions, explicit 1/2/3/4 active-fan count for four-fan cases, plant-instance perturbations, left/right aileron asymmetry, feedback latency, command timing, and actuator delay. Plant-instance perturbations mean mass scale, CG-offset moment-arm shift, diagonal inertia scaling, strip cd0 scale, and flap-scale surface calibration; cross-inertia is logged as not perturbed. No hidden LQR retuning.
- Post-W3 library-size cross-study: the heavy, balanced, light, super-light, and no-clustering/no-merging comparison of W3-eligible primitive-controller variants before any validation library-size condition is accepted.
- Full-loop validation: frozen post-W3 library-size condition + outcome model + viability governor + selector + directional 3D residual memory, tested across W0--W3 and then in real repeated launches.
- Real flight: fixed-gate repeated-launch validation with Vicon-measured initial states.
- Deadline: 15 June 2026.
- Writing protection: 08--15 June is protected for thesis writing and final integration.

Global execution rules
----------------------
- The active primitive inner loop is time-invariant LQR only.
- The LQR stabiliser is part of each primitive-controller variant, not a free-standing controller bank.
- Do not implement TVLQR in the active workflow.
- Do not preserve the previous PD-like bounded controller as a fallback, baseline, or ablation.
- Do not use LQR-tree or funnel-library terminology to brand the method.
- If LQR synthesis fails for a primitive, label the controller or rollout as blocked; do not substitute PD.
- Do not resurrect fan-layout-specific controller logic.
- Do not make reachable-state chaining a required success gate.
- Single-fan, four-fan, shifted-fan, and fan-power cases are environment instances, not separate algorithms.
- W labels are fidelity/evidence layers only, not online algorithm branches.
- Strict evidence ladder: R5 W0/W1 is dry-air plus annular-GP randomized synthesis/training; W2 is optional GP-corrected annular-Gaussian diagnostic only; W3 is frozen held-out randomised GP-corrected annular-Gaussian validation.
- W0/W1 generate and tune a rich primitive-controller library; they do not output a 12--24 retained shortlist.
- W3 replays fixed primitive-controller variants and shrinks only by held-out survival evidence; it does not regenerate, retune, or mutate LQR gains, Q/R weights, horizons, references, or entry sets. Optional W2 follows the same no-retune rule but has no required move-on authority.
- Clustering and merging happen only after W3.
- Controlled x-y arena exits with positive floor and ceiling margin are retained as `episode_terminal_useful` / `safe_terminal` evidence; they are not continuation-valid states and must not be deleted or marked as hard failure merely because a launch leaves the finite arena.
- Dense runs must use chunked, resumable, compressed, worker-enabled execution.
- Dense-run default: workers=8, max_workers=8, storage_format=auto/parquet/csv_gz, compression_level=1, resume=true.
- No generated file may exceed 100 MB; preferred limit is 75 MB.
- Real-flight transfer claims require matched real logs and sim-real replay.

Execution table
---------------

| Date | Part | Stage | Run target | Fidelity / environment target | Runtime and storage target | Quantitative target | Output by end of day | Move-on check |
|---|---|---|---|---|---|---:|---|---|
| Fri 22 May | L0 | Repository reset and PD run retirement | Model-only cleanup plus archive/delete previous bounded-PD dense run | N/A. Retain glider, panel-wise model, latency, updraft, runtime/storage, tests, docs | No dense run | Retirement manifest covers previous dense-run outputs and active PD controller paths | Historical retirement note absorbed into `Glider_Control_Project_Plan.md`; clean active tree | No previous dense-run result, PD controller path, retired archive, or chain-formation path remains active |
| Sat 23 May | L1 | Foundation and timing-aware model audit | Model audit plus trim/local-reference and linearisation smoke | W0 dry-air smoke; historical W1 Gaussian smoke is diagnostic-only; panel-wise wind, feedback latency, command timing, and actuator lag active from this stage | Smoke only; no dense run | All retained model files inspected; at least one controllability/Riccati solve attempted | Historical foundation note absorbed into `Glider_Control_Project_Plan.md`; timing/model audit report | Frame, unit, sign, trim, linearisation, Riccati, panel-wise wind, latency, command-timing, or actuator-lag mismatch is fixed or explicitly blocked |
| Sun 24 May | L2 | Environment context and mixed start-state sampler | Context encoder plus launch/in-flight primitive-start sampler | W0 dry air; active W1 annular-GP training; W2 annular-GP availability check; W3-style shifted fan and power-scaled context smoke | Smoke table; file <100 MB | Mixed start-state distribution implemented: 40% launch_gate, 25% inflight_nominal, 15% inflight_lift_region, 10% inflight_boundary_near, 10% inflight_recovery_edge | `env_ctx.py`; `prim_start_sampler.py`; `ctx_smoke.csv`; `docs/abbr.md` | Context features and provenance fields work for dry, diagnostic Gaussian, annular-GP, shifted, and power-scaled environments |
| Mon 25 May | L3 | Primitive catalogue and variant schema | Primitive and controller metadata smoke | Environment-independent definitions; W0/W1 execution assumptions include panel-wise and timing models | Smoke only | 8 active primitive families; each has declared primitive-controller fields | `prim_cat.py`; primitive schema tests | Every primitive has entry set, reference, finite horizon, LQR metadata, exit checks, metrics, failure labels, and claim status |
| Tue 26 May | L4 | LQR synthesis and timing-aware rollout smoke | First primitive-controller variants for active primitive families | W0 dry-air and active W1 annular-GP contexts; panel-wise wind, feedback latency, command timing, and actuator lag active | Smoke only; no dense run | At least one solved or explicitly blocked LQR controller per primitive family | `lqr_controller.py`; `lqr_linearisation.py`; LQR/timing smoke manifest | No primitive silently falls back to PD; each failure has a blocked reason |
| Wed 27 May | L5 | R5 robust synthesis preflight | State + context + primitive + controller rollout rows | R5 W0/W1 required; no W3 filtering yet | Chunked preflight; workers<=8; csv_gz/parquet; checksums; no file >100 MB | 1k--5k preflight rows covering all five transition-entry start families | `run_lqr_w01_dense_chunked.py`; table manifest; runtime summary | Chunking, resume, repair, checksums, file-size audit, LQR metadata, timing fields, transition-entry separation, and boundary-use labels pass |
| Thu 28 May | L6 | R5 robust transition-object Q/R plus attitude/bank reference-bias training I | Tune primitive-local variants inside each primitive using mixed primitive-start states and W3-style randomised training | W0 dry/near-dry + W1 annular-GP randomized single/multi-fan cases; dry rows keep no-updraft environment but use the same W3-style plant/implementation perturbations; no Gaussian pass evidence; no launch-special branch | Dense if >10k rows: workers=8, max_workers=8, chunked, compressed partitions | Required rich library: active primitives x 32 structured Q/R plus attitude/bank reference-bias candidates x 100 paired tests, using local-speed scheduled LQR bins from 2.0 to 9.0 m/s without hard speed chasing | Primitive-controller variant registry; Q/R/reference diagnostics; transition candidate training summary; failure-label summary | Every generated variant is retained as audit evidence; row count and local accepted rows cannot pass R5 alone |
| Fri 29 May | L7 | R5 robust transition-object Q/R plus attitude/bank reference-bias training II and freeze audit | Complete W0/W1 dense synthesis and prepare selected frozen transition objects for held-out W3 | W0/W1 randomized synthesis only; include accepted, weak, failed, rejected, continuation-valid, and x-y terminal-useful rows | Dense run continues with resume/repair; compressed partitions | Required dense target: 76,800 rows = 8 active primitives x 32 candidates x 3 environments x 100 paired tests, with 40/25/15/10/10 start-family split | W0/W1 rich library manifest; `r5_transition_candidate_training_summary.csv`; `r5_transition_selected_for_r7.csv`; `r5_transition_pareto_front.csv`; transition training manifest; frozen selected bundle | W3 is cleared only when R5 selected transition objects exist for launch_gate and inflight_stable for every active primitive, seed separation is recorded, and unselected candidates remain audit-only |
| Sat 30 May | L8 | Optional W2 annular-GP diagnostic sweep | Replay the fixed R5 library under hardware-aware realism for diagnosis only | W2: single-fan and four-fan annular-GP, panel-wise wind, feedback latency, command timing, actuator lag, realistic termination | Dense if >10k rows: workers=8, chunked, compressed | Preferred 20k--60k W2 rows if requested; fallback 10k | W2 diagnostic table; eliminated/downgraded/surviving variant summary; no-retune audit | W2 cannot unblock R8/R9/R10/R11; any W2 failure is diagnostic and does not weaken W3 holdout gates |
| Sun 31 May | L9 | R7 dry-air plus W3 frozen held-out dense survival sweep | Stress frozen R5 variants under held-out randomised environment, implementation, timing, launch, and plant uncertainty | R7: W0 dry-air zero-wind plus W3 single-layer composed annular-GP, fan position, fan number, per-fan power, updraft width/spread, residual uncertainty, mass scale, CG-offset strip moment-arm shift, diagonal inertia scale, strip cd0 scale, flap-scale surface calibration, latency jitter, command timing, actuator delay; no duplicate amplitude/centre randomisation wrapper; no cross-inertia perturbation | Dense if >10k rows: workers=8, chunked, compressed | Preferred 30k--100k R7 rows; fallback 15k | R7 survival table grouped by primitive_id and transition_entry_class; strict-survivor, route-usable, recovery-route-usable, weak, and fail labels; randomisation and seed-separation manifest; no-retune audit | R7/W3-eligible transition objects move to post-W3 library-size cross-study; blocked/downgraded reasons are explicit and all active primitives must retain launch_gate route-usable or survivor evidence before R8 acceptance |
| Mon 01 Jun | L10 | Post-W3 library-size cross-study | Select coverage-aware medoids from the W3-eligible transition-object library | Post-W3 only; use labelled weak/failure/terminal evidence for boundary explanation | Compact reports; dense inputs remain partitioned | Five size cases are compared: heavy, balanced, light, super-light, and no-clustering/no-merging; heavy chooses the best worst-case-coverage medoid, balanced/light/super-light use greedy marginal coverage, and no-cluster keeps all eligible transition objects | Final primitive library registry; medoid selection audit; failure-cluster summary; hardware-candidate shortlist | Selection groups by primitive_id and transition_entry_class, uses existing W3-eligible objects only, keeps continuation-valid separate from episode-terminal-useful cases, and runs without averaging Q/R or synthesising new controllers |
| Tue 02 Jun | G1 | Outcome model, governor, selector, and directional 3D residual memory | Train/use the outcome model, viability governor, selector, and directional 3D residual lift belief over the post-W3 selected library-size condition | Frozen selected library-size condition for integration; no hidden controller mutation | Chunked episode logs if dense; compact metrics | Outcome model plus governor smoke; residual-memory smoothing in {0.0, 0.5, 0.8, 0.95} | Outcome model report; governor rejection logs; selector and belief smoke results | Governor can select primitives after a previous primitive ends, log rejection reasons, distinguish continuation_mode from terminal_episode_mode, use heading-aware front/left/right wall clearance for preselection, retain all-wall rollout hard failures, exclude low speed from governor rejection, recovery routing, scoring, and audit gates, and treat energy loss as audit-only rather than hard-failure evidence |
| Wed 03 Jun | V1 | Full-loop simulation validation | Test the entire outer loop across dry air and annular-GP randomized conditions | W0 dry air; W1 annular-GP training evidence; W2 optional annular-GP diagnostic; W3 held-out domain randomisation; frozen library, governor, selector, memory, R10 hard full-domain tuning, and R11 fidelity-ladder validation | Worker-enabled repeated-launch validation; parallel across independent final held-out schedule rows, history sequential inside each worker, parent-owned chunked partitions; no primitive-count cap in full validation, with a 20 s per-episode simulation safety budget for R10/R11 and a 10 s internal R9 preflight budget | R9: internal quick preflight, 5 library cases x 3 fixed blocks with no-memory plus h3/h10/h30 memory = 60 final launches plus 645 history launches; R10: one full-domain arena-wide training block with 50 outer cases per condition, active fan count 0/1/2/3/4, fan centres at least 1.0 m apart, and W3 plant/implementation sampled once per outer case; R11: eight 10-case held-out fidelity blocks = 80 total outer cases from dry-air fixed through full-domain arena-wide randomisation | No-memory versus recency-weighted candidate-path directional-memory comparison with shielded uncertainty exploration built into memory policies; standalone h100 or safe-explore variants are appendix ablations | Safe primitive execution, prediction alignment, memory benefit/no-benefit, dwell, energy, margin, termination, selected history/final trajectories, and compact candidate rejection summaries are reported |
| Thu 04 Jun | H1 | Hardware shortlist and arena readiness | Real-flight readiness using post-W3 selected library-size condition | Real no-updraft/fan-off target first; Vicon start trigger and abort logic checked | No dense run; real logs <100 MB each | 5--10 candidates; 3--5 shakedown launches if safe | Hardware shortlist; abort rules; logging checklist; shakedown log | Supervisor can understand which LQR primitives will be flown and why they are safe to attempt |
| Fri 05 Jun | H2 | Real no-updraft baseline | Dry/fan-off repeated launches | Real W0 fan-off or dry baseline with Vicon-measured initial states | Real logs partitioned if needed; every file <100 MB | Minimum 6 usable launches; target 8--10 | Baseline real-flight table; matched W0 replay setup | Baseline outcomes are usable for comparison; x/y boundary exits are logged as terminal outcomes, not deleted |
| Sat 06 Jun | H3 | Real updraft and memory experiments | Environment-conditioned primitive selection and directional 3D residual memory | Real single-fan and four-fan measured-updraft cases; changed fan power or subset only if safe and approved | Real logs with episode IDs and belief snapshots; no file >100 MB | Minimum 6 usable launches per approved condition; target 8--10 | Contextual selector table; belief logs; episode comparison table | Logs include measured state, selected primitive, controller ID, rejection log, belief before/after, and termination cause |
| Sun 07 Jun | H4 | Changed-environment test, sim-real pairing, and results lock | Replay every usable real launch and lock claim boundary | Changed fan number/position and added mass only if safe and approved; matched W1/W2-compatible replay; W3-style replay for robustness discussion | Chunked replay if dense; compact paired metrics; manifest and file-size audit | Every usable real launch has matched replay | Sim-real prediction table; results package; figure-source manifest; claim-boundary note | Transfer labels are metric-specific and claim-safe; no new experiments unless a manifest, safety, or logging error is found |
| Mon 08 Jun | W1 | Methods writing | Chapter 6 methods | R5 robust W0/W1 synthesis, optional W2 diagnostics, frozen W3 survival, post-W3 library-size cross-study, governor, selector, and memory described in order | Writing only | Method skeleton and algorithms complete | Chapter 6 draft | Text matches implemented code and avoids retired branch/chain/PD story |
| Tue 09 Jun | W2 | Results writing | Chapter 7 results | Report R5 robust rich library, optional W2 diagnostics if run, frozen W3 survival, post-W3 library-size cross-study, full-loop simulation, real flight, and sim-real pairing | Writing only | Core tables and figures integrated | Chapter 7 draft | Results use supported / partial / negative / not-tested labels |
| Wed 10 Jun | W3 | Introduction and related work | Chapter 1/2 rewrite | Position project as feedback-stabilised primitive sim-to-real transfer, not perching or all-arena exploration | Writing only | Intro and related-work draft | Chapter 1/2 updated | No old final-objective trace remains |
| Thu 11 Jun | W4 | Platform/updraft/model chapters | Chapters 3--5 alignment | Explain retained glider, panel-wise, timing, and updraft/local-flow foundations | Writing only | Platform, updraft, and glider chapters coherent | Chapter 3--5 edits | Assumptions match the final method |
| Fri 12 Jun | W5 | Figures and captions | Thesis figures | Figures cover R5 robust synthesis, optional W2 diagnostics if run, frozen W3 survival, post-W3 library-size cross-study, full-loop validation, and real evidence where available | Figure files <100 MB; preferred <75 MB | All core figures traceable | Figure folder and caption draft | Every figure has data source and claim boundary |
| Sat 13 Jun | W6 | Full thesis integration | Compile all chapters and appendices | Whole thesis | Writing only | Full compile; missing refs listed | Full draft PDF | No major missing section remains |
| Sun 14 Jun | W7 | Review and compression | Clarity, consistency, claim safety | Whole thesis | Writing only | Remove repetition and retired strategy traces | Near-final PDF | Supervisor-readable and internally consistent |
| Mon 15 Jun | W8 | Final checks | Submission package | Whole thesis and repository evidence package | File-size, reference, compile, and reproducibility audit | Zero critical compile errors | Submission-ready thesis package | Submission package complete |

Move-on hierarchy
-----------------
1. Model, sign, trim, panel-wise wind, feedback-latency, command-timing, or actuator-lag mismatch blocks new LQR controller evidence.
2. Missing LQR synthesis and audit blocks W0/W1 rich primitive-library generation.
3. Missing R5 W0/W1 robust frozen primitive-controller library blocks W3.
4. Missing W3 held-out survival blocks post-W3 library-size cross-study and robust-library claims.
5. Missing W3-eligible launch_gate entry transition evidence for the active primitive families blocks R8/R10/R11.
6. Missing post-W3 library-size cross-study blocks the compact governor library and realistic real-time selection claim.
7. Missing full-loop simulation validation blocks claims about outer-loop governor and memory behaviour.
8. Missing matched sim-real replay blocks transfer claims.
9. Missing real flight still allows simulation-only thesis claims if labelled honestly.
10. Reintroducing PD as an active controller, selecting a 12--24 W0/W1 shortlist, clustering before W3, or using W2 diagnostic survival as an accepted move-on gate is not a valid unblock path.

Evidence wording
----------------
Use these labels in reports and thesis tables:

```text
simulation_only
hardware_shakedown
real_flight_evidence
partial_transfer
negative_transfer
instrumentation_limited
not_tested
retired_not_active
blocked_lqr_synthesis
eliminated_w2_survival
eliminated_w3_survival
post_w3_cluster_representative
```

Do not claim:

```text
mission success
full autonomy
real-flight transfer
full W3 robustness
environment generalisation
hardware readiness
formal LQR-tree, funnel, or verified region-of-attraction guarantees
```

unless the exact evidence exists and is paired with its limitations.
