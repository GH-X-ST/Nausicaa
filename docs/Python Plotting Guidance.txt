# Python Plotting Guidance — Robotics-Journal Figures

<!-- R9_LAUNCH_GATE_ALIGNMENT_START -->
## Current controlling update - robust R5 synthesis and frozen W3 holdout

This document is aligned with `docs/Glider_Control_Project_Plan.md`, which is the single method-control document for the current implementation pass.

Current decision:

```text
BLOCKED_FIX_REQUIRED until the repaired R5 -> R7 -> R8 -> R9 -> R10 evidence chain passes.
```

Required repair direction:

```text
1. Restart accepted evidence from robust R5 dry-air plus annular-GP synthesis; older R5/R7/R8/R9/R10 roots remain diagnostic unless regenerated under the current contract.
2. R5 dry-air rows keep the W0 no-updraft environment but use W3-style plant and implementation perturbations; R5 annular-GP rows use the same W3-style plant/implementation perturbation family.
3. R5 and R7 use single-layer composed annular-GP randomisation: fan position, active fan count where applicable, per-fan power, width/spread, local uncertainty, launch/start-state perturbations, latency/implementation perturbations, left/right aileron asymmetry, and CG/plant perturbations.
4. Do not duplicate randomisation channels: per-fan power is updraft strength, fan position is spatial/layout shift, width is spread, local uncertainty is context/belief uncertainty, amplitude stays fixed at 1.0, and centre shift stays zero for active pass-gated annular-GP evidence.
5. Keep W2/R6 optional diagnostic only; R7 is the frozen held-out W3 fixed-LQR replay with no Q/R, K, reference, horizon, entry-role, controller-ID, or primitive-variant-ID mutation.
6. Keep post-W3 reduction as the four-case library-size cross-study: heavy, balanced, light, and no-clustering/no-merging.
7. R9 is fixed-case repeated-launch validation with no glider, latency, actuator, or environment randomisation beyond controlled launch/history sampling.
8. R10 is environment-only changed-case validation with six 20-case blocks: nominal single, nominal four, shifted single, shifted four, active-fan-number variation, and arena-wide fan-position plus active-fan-count generalisation.
9. R9/R10 must use controlled final-launch pairing, compact-library-aware outcome lookup, actual memory/exploration selection-change metrics, and secondary launch-score analysis only after strict gates pass.
10. No memory improvement, hardware readiness, real-flight transfer, mission success, full autonomy, or formal LQR-tree/funnel/ROA claim is allowed until full R9 and R10 gates pass.
```

Forbidden changes remain unchanged: no PD/PID/bounded fallback, no TVLQR active workflow, no fan-layout-specific controller logic, no pre-W3 clustering, no W3/R8/R9/R10 retuning, no deletion of useful x-y terminal boundary evidence, no direct surface-command RL, no nonlinear MPC substitution, and no LQR-tree/funnel-library claim.
<!-- R9_LAUNCH_GATE_ALIGNMENT_END -->

## Purpose

Use this guide when creating plots for thesis, slides, logs, or robotics-style paper figures. Figures should show physical evidence, not decorative output.

## General style

Use:

- white background;
- clear axes and units;
- restrained colours;
- readable labels;
- deterministic export;
- captions that state the claim;
- high-resolution PNG for thesis figures.

Recommended export:

```python
fig, ax = plt.subplots(figsize=(5.2, 3.0), dpi=600)
fig.patch.set_facecolor("white")
ax.set_facecolor("white")
...
fig.savefig(out_path, dpi=600, facecolor="white", bbox_inches="tight")
plt.close(fig)
```

## File-size rule

Every generated plot file should remain below 100 MB. Prefer below 25 MB for thesis figures and below 75 MB for dense diagnostics. If a plot exceeds 100 MB, simplify it, rasterise dense layers, split it into panels, or move full data to compressed partitions.

## Figure types needed

### 1. Environment context map

Show:

- arena outline;
- local updraft field;
- context features such as wing-mean wind or spanwise gradient;
- fixed launch gate or representative current state;
- safety volume.

### 2. Primitive outcome map

Show accepted, weak, failed, and rejected primitive outcomes over physically meaningful axes:

```text
speed
heading error
wing-mean wind
left-right wind difference
wall margin
energy residual
lift dwell time
```

Also show `episode_terminal_useful` or `boundary_use_class == "episode_terminal_useful"` separately when x/y wall or lateral safety-volume exits are retained as terminal episode evidence. Do not colour these rows as continuation success.

### 3. Repeated-launch episode traces

Show multiple episodes with:

- trajectory path;
- accepted primitive;
- termination point;
- local updraft contour;
- belief before/after where useful.

Do not overlay too many episodes in one plot.

### 4. Belief evolution

Show:

- belief before and after repeated launches;
- observed lift evidence;
- decay setting;
- capture or dwell metric over launch index.

### 5. Sim-real pairing

Show predicted versus measured:

- energy residual;
- lift dwell time;
- capture success;
- termination cause;
- minimum wall margin.

### 6. Generalisation test

Show fidelity-layer and environment-instance effects without turning them into online branches:

- shifted fan position;
- changed fan power;
- active fan subset;
- randomised updraft amplitude or width.

Use W0 as dry air only. For active pass-gated R5/R7 evidence, use W1 as annular-GP randomized training and W3 as held-out randomised GP-corrected annular-Gaussian validation; Gaussian plume plots are diagnostic-only. W2 remains optional GP-corrected annular-Gaussian diagnostic replay. Do not present W0/W1 clustering as the final library-compression result.

### 7. Post-W3 library compression

Show clustering and merging only after W3 survival filtering. Figures should distinguish:

- W0/W1 generated variants;
- W2 eliminated or downgraded variants;
- W3 eliminated or downgraded variants;
- W3 survivors selected as medoids or merged representatives;
- continuation-valid versus episode-terminal-useful cases.

## Colour guidance

Use consistent semantic colours:

```text
accepted / safe: green
weak / marginal: amber
failed / unsafe: red
rejected before execution: grey
baseline: blue or dark grey
context-conditioned controller: green or purple
```

Do not use rainbow maps for categorical outcomes.

## Label rules

Every axis must include units where applicable:

```text
x_w (m)
y_w (m)
z_w (m)
speed (m s^{-1})
yaw error (deg)
energy residual (m)
lift dwell time (s)
minimum wall margin (m)
wing-mean vertical wind (m s^{-1})
```

## Caption standard

Good caption:

```text
Primitive outcome map under measured-updraft replay. The accepted region expands with wing-mean vertical wind, while low-speed and low-margin cases remain rejected by the viability governor.
```

Poor caption:

```text
Results of simulation and real flight.
```

## Audit metadata

Each saved plot should have a nearby source note or manifest stating:

```text
input files
filtering rules
sample count
environment_id
latency case
policy name
created date or run ID
file size
```

### Launch-gate first-decision audit

For the current R9 repair, include figures or tables for:

- launch-gate admissible candidate count;
- viable first-decision candidate count;
- rejection reason distribution;
- selected launch-capture primitive family;
- transition-to-inflight success;
- no-viable primitive rate before and after repair.

Do not plot the blocked R9 result as a pass result. Mark it as `diagnostic_not_passed`.
