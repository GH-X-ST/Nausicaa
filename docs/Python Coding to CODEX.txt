# CODEX Instruction 鈥?Python Implementation for Nausicaa

<!-- R9_LAUNCH_GATE_ALIGNMENT_START -->

## Active Transition-Aware Thesis Workflow

The active thesis workflow is `R5 -> R7 -> R8 -> R10 -> R11 -> Reality`. R9 remains internal preflight only and is not thesis-facing evidence. R10 tunes the viability governor with residual updraft adaptation, and R11 is the held-out validation gate.

Launch is an entry regime, not a separate controller family. The active primitive catalogue has exactly eight manoeuvre families: `glide`, `recovery`, `lift_entry`, `lift_dwell_arc`, `mild_turn_left`, `mild_turn_right`, `energy_retaining_bank`, and `safe_exit_or_recovery_handoff`. Retired `launch_capture_*` IDs are archive aliases only and must not appear in active evidence.

Every primitive is treated as a transition object. R5 is robust transition-aware primitive / transition-object Q/R plus primitive-reference-bias training across five entry start families with exact dense proportions per primitive/candidate/environment: 40 `launch_gate`, 25 `inflight_nominal`, 15 `inflight_lift_region`, 10 `inflight_boundary_near`, and 10 `inflight_recovery_edge`. The dense evaluation target remains `8 * 32 * 3 * 100 = 76,800` rows, but row count alone is not a pass condition. Candidate 0 is nominal, candidates 1-7 are named physical anchors with small interpretable reference biases, and candidates 8-31 are deterministic Latin-hypercube log multipliers over the seven grouped LQR weights plus bounded pitch, bank, and speed reference biases. R5 writes `r5_transition_candidate_training_summary.csv`, `r5_transition_selected_for_r7.csv`, `r5_transition_pareto_front.csv`, and `r5_transition_training_manifest.json`, then freezes only selected transition objects for R7 while keeping the full candidate bundle as audit evidence.

R7 is held-out transition validation of the frozen R5-selected transition objects. No primitive may pass R7 solely on local rollout success; no primitive may pass R5 or R7 from dense row count or aggregate primitive success across entry classes. A controller can survive for one `primitive_id + entry_class` and fail for another. R8 compresses transition objects grouped by `primitive_id` and `transition_entry_class` using coverage-aware medoid selection without averaging Q/R, K, references, or controller IDs.

The governor classifies the current state, filters representatives by validated `transition_entry_class`, rejects high hard-failure risk, scores transition probability plus updraft gain plus flight time plus residual-memory correction, executes the best transition object, and updates case-local residual memory. Step 0 has `current_state_class = launch_gate`, so it selects only transition objects validated for `entry_class = launch_gate`; there is no launch-specific primitive family route.

Residual memory is a small case-local modifier: `predicted_updraft_gain = library_prediction + residual_memory_correction`. It must not override state classification or entry/exit compatibility. Memory is reinitialised per final test row. Final scoring is computed only from the final held-out rollout path. There is no hidden speed gate, no energy-loss hard failure, no PD/PID, no TVLQR, and no fan-layout-specific controller logic.

Core comparison is no memory versus residual-memory histories such as h5, h20, and h100, with safe-explore only as optional ablation. Hardware readiness, real-flight transfer, mission success, autonomy, and memory-improvement claims require R11 and later real-flight evidence.

<!-- R9_LAUNCH_GATE_ALIGNMENT_END -->

## Role

You are editing Python code for a research-grade robotics/aerospace simulation and experiment pipeline. Implement only the requested change while preserving physical conventions, rich comments, runtime efficiency, storage discipline, and auditability.

## Current method

Use **primitive**, not skill.

The active direction is transition-aware primitive selection:

```text
state + local flow context + primitive parameters
    -> primitive outcome estimate
    -> transition viability governor
    -> selected 0.10 s primitive
    -> entry_class -> exit_class transition log
    -> episode log
    -> directional 3D residual updraft-gain update
```

Fan layout, fan power, W layer, and reachable primitive chains are fidelity/evidence structures. They are not the online controller.

Primitive-local LQR variants are part of the active method. The LQR stabiliser is embedded inside each primitive. W0/W1 dense generation develops a rich primitive-controller library and tunes each variant's local LQR using the panel-wise glider model, feedback latency, command timing, and actuator lag. W2 is diagnostic-only. R7/W3 replays fixed variants to eliminate or downgrade failures without retuning, and no primitive may pass R7 solely from local rollout success. Clustering and merging occur only after R7 and must not be moved earlier. Any future redesigned controller is a separate W0/W1 learning cycle, not an in-place holdout mutation.

Transition entry classes should be explicit when they affect evidence: `launch_gate`, `inflight_stable`, `boundary_near`, and `recoverable_degraded`. Launch-gate failure for one transition object is not, by itself, a blocker for the same controller's in-flight transition object.


## Before editing

1. Identify the exact files allowed to change.
2. Inspect current public interfaces, imports, state order, command order, units, and file-size behaviour.
3. Identify which outputs become evidence: manifest, table, plot, report, or log.
4. Do not edit unrelated code.
5. Do not reset or overwrite existing result directories unless explicitly authorised.

## Coding style

Use rich comments. Comments should explain physical assumptions, context features, evidence boundaries, unit/frame conventions, and why a gate or metric exists.

Prefer:

- type hints;
- dataclasses for structured records;
- explicit unit suffixes;
- deterministic seeds;
- narrow helpers;
- explicit validation at module boundaries;
- `Path` for file paths.

Avoid:

- speculative frameworks;
- generic configuration systems;
- hidden unit conversion;
- silent frame conversion;
- broad try/except that hides failures;
- new dependencies unless justified.

## Runtime and storage rule

Any dense/archive/thesis-scale run must be:

```text
chunked
resumable
compressed
worker-enabled
checksum-manifested
file-size audited
```

A generated file must stay below 100 MB unless explicitly local-only and approved. Dense table data must be partitioned under `tables/`. Root or `metrics/` outputs should be compact summaries only.

Do not create or use a full-memory single-process runner for dense evidence.

## Evidence and claims

Every output should state whether it is:

```text
simulation_only
hardware_shakedown
real_flight_evidence
partial_transfer
negative_transfer
instrumentation_limited
not_tested
```

Do not claim real-flight transfer, mission success, hardware readiness, all-environment validity, or full W3 robustness unless direct evidence supports it.

## Current implementation priorities

Prefer changes that support:

- environment-context feature extraction;
- compact primitive catalogue;
- primitive outcome datasets;
- viability-filtered primitive selection;
- repeated-launch episode records;
- decaying directional 3D residual lift-belief update;
- W0/W1 rich primitive-library generation, W2/W3 survival filtering, and post-W3 library-size cross-study as evidence layers;
- real-flight replay pairing;
- chunked archive execution and compact file management.

Do not make high-angle agile turns or within-flight reachable chaining a required final mission dependency.

## Validation

Run the narrowest meaningful checks:

- import check;
- unit test;
- schema check;
- small deterministic sample;
- row-count and manifest check;
- file-size audit;
- no-overwrite check.

If a check cannot run, report why. Do not imply that it passed.

## Final response

Report:

1. files changed;
2. public interfaces added or preserved;
3. tests/checks run;
4. evidence files generated;
5. claim boundaries preserved;
6. remaining risks.
